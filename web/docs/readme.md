# aufbau/web/docs

the docs of aufbau.dev, built by `@aufbau/builders/docs`. aufbau.dev serves `web/`
only, so the markdown of the repo is read from `https://code.pulgasari.dev/aufbau/`
through the `$repo` var. `packages.md` and `resources.md` live here.
